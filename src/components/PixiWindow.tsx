import { FC } from "react";
import { Stage, AnimatedSprite, Container } from "@inlet/react-pixi";

const PixiWindow: FC = () => {
  return <Stage>{/* ここに Pixi Component */}</Stage>;
};

export default PixiWindow;
